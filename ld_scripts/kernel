
OUTPUT_FORMAT(elf32-i386)

INPUT
(
	video.o
	io.o
	memory.o
)
STARTUP(kernel.o)

OUTPUT(KERNEL)
ENTRY(kmain)

SECTIONS {
	. = 0x7000;
	.text : {
		*.o (.text);
	}
	.data : {
		*.o (.data);
	}
	.bss : {
		*.o (.bss);
	}
}
