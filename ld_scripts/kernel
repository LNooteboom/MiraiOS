
OUTPUT_FORMAT(elf32-i386)

/*INPUT
(
	btst2.o
	kernel.o
)*/
STARTUP(kernel.o)

OUTPUT(KERNEL)
ENTRY(kmain)

SECTIONS {
	. = 0x0000;
	.text : {
		kernel.o (.text);
	}
	.data : {
		kernel.o (.data);
	}
}
